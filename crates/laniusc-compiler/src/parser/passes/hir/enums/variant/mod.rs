/// Links enum variant rows to parent enum declarations.
pub mod links;
/// Ranks enum variant and payload rows.
pub mod rank_step;
/// Scatters ranked enum variant rows into compact storage.
pub mod scatter;
