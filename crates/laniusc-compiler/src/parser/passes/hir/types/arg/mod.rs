/// Links type argument rows to their owning type node.
pub mod links;
/// Ranks type argument rows within each type-argument list.
pub mod rank_step;
/// Scatters ranked type argument rows into compact storage.
pub mod scatter;
