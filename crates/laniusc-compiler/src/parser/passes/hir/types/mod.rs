/// Type-alias owner and target passes.
pub mod alias;
/// Type-argument list passes.
pub mod arg;
/// Type form and type field extraction pass.
pub mod fields;
/// Type path leaf ownership passes.
pub mod path;
/// Topmost type-expression root ownership.
pub mod root;
