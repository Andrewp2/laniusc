/// Links struct field rows to parent struct declarations.
pub mod links;
/// Ranks struct field rows within each struct declaration.
pub mod rank_step;
/// Scatters ranked struct field rows into compact storage.
pub mod scatter;
