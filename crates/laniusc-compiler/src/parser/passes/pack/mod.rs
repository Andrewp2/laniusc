/// Prefix-scan passes for variable-length parser stream offsets.
pub mod offsets;
/// Reduction passes for total packed stream sizes.
pub mod totals;
/// Scatter pass for packed variable-length parser streams.
pub mod varlen;
